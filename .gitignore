node_modules/
.shadow-cljs/
# test/browser/verify_http_post_browser.cljs generates its guest fixture
# fresh each run (the target URL's port isn't known until its local echo
# server is listening) -- never checked in.
test/browser/.tmp/
